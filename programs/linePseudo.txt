# Initiate Robot
LET sensor = readLineFollower
# Move forward until on a line
WHILE sensor == BOTHW:
	MOVE_FORWARD
	sensor = readLineFollower
# Try to stay on the line
# Turn 180° if off the line
WHILE TRUE:
	IF  sensor == LEFTB:
		TURN_LEFT
	ELIF sensor == RIGHTB:
		TURN_RIGHT
	ELIF sensor == BOTHB:
		MOVE_FORWARD
	ELIF sensor == BOTHW:
		TURN_AROUND 
	sensor = readLineFollower